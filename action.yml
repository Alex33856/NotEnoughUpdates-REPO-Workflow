name: "NotEnoughUpdates-REPO-Workflow"

description: "Validate all changed files."

inputs:
  repo-token:
    description: "Token with permissions to do repo things"

runs:
  using: "node24"
  main: "dist/index.js"
